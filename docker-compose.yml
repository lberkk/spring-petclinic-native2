services:
  graalvm:
    build:
      context: .
      dockerfile: Dockerfile
    image: graalvm-rocky:local
    working_dir: /workspace
    volumes:
      - ..:/workspace
    
volumes:
  maven-repo:
